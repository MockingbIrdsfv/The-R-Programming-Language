变量赋值
a<-10
b<-"good"
f<-c(1,2,3)
ps: 赋值符号：<-
    c()组合多个函数
运算
+   加
-   减
*   乘
/   除
^   幂
%%  求余
%/% 整除 
<   小于
<=  小于等于
==  等于
!=  不等于
!   非
|   或
&   且
控制结构
if else
         {
         if(条件)
         else if(条件)
         else
         }
while    
         while(条件)
         {
         执行语句
         }
for      
         for(i in x)
         {
         对于x数据集中的每一个元素依次循环进行操作
         } 
访问数据
       [] 从1开始         
